function getCharaOffset() return 0 end
function setInfos(player, name, title, dan, data) end
function drawDan(x, y, opacity, danGrade, titleTex) end
function drawTitlePlate(x, y, opacity, type, titleTex, rarity, nameplateId) end
function update(...) end
function draw(x, y, opacity, player, side) end

function reloadLanguage(lang)
end

function loadAssets()
end
